import { Loader2 } from "lucide-react";

const Loading = () => {
  return (
    <Loader2 className="left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 h-4 w-4 animate-spin" />
  );
};

export default Loading;
